# 🚀 设置国内镜像加速指南

## 📋 方法一：设置 npm 镜像（推荐）

### 步骤：

1. **打开终端**（Terminal）

2. **设置 npm 镜像**：
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```

3. **验证是否设置成功**：
   ```bash
   npm config get registry
   ```
   应该显示：`https://registry.npmmirror.com`

4. **现在可以正常使用 npm**：
   ```bash
   npm install
   npm run build
   ```

## 📋 方法二：设置 Electron 镜像（加速 Electron 下载）

### 步骤：

1. **临时设置（当前终端会话有效）**：
   ```bash
   export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
   npm run build
   ```

2. **永久设置（推荐）**：
   
   **macOS/Linux**：
   ```bash
   # 编辑或创建 ~/.zshrc 或 ~/.bash_profile
   echo 'export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"' >> ~/.zshrc
   
   # 重新加载配置
   source ~/.zshrc
   ```
   
   **Windows**：
   ```cmd
   # 在 PowerShell 中运行
   [System.Environment]::SetEnvironmentVariable('ELECTRON_MIRROR', 'https://npmmirror.com/mirrors/electron/', 'User')
   ```

## 📋 方法三：同时设置两个镜像（最完整）

### 一次性设置：

```bash
# 1. 设置 npm 镜像
npm config set registry https://registry.npmmirror.com

# 2. 设置 Electron 镜像（临时，当前终端）
export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 3. 验证
npm config get registry
echo $ELECTRON_MIRROR
```

## 🔍 如何检查是否设置成功？

### 检查 npm 镜像：
```bash
npm config get registry
```
**应该显示**：`https://registry.npmmirror.com`

### 检查 Electron 镜像：
```bash
echo $ELECTRON_MIRROR
```
**应该显示**：`https://npmmirror.com/mirrors/electron/`

## 🔄 如何恢复默认镜像？

如果以后想恢复使用官方源：

```bash
# 恢复 npm 默认源
npm config set registry https://registry.npmjs.org/

# 取消 Electron 镜像（临时设置会自动失效）
unset ELECTRON_MIRROR
```

## 💡 快速操作步骤（复制粘贴）

### macOS/Linux 用户：

```bash
# 1. 设置 npm 镜像
npm config set registry https://registry.npmmirror.com

# 2. 设置 Electron 镜像（临时）
export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 3. 验证
npm config get registry

# 4. 现在可以安装和打包了
npm install
npm run build
```

### Windows 用户（PowerShell）：

```powershell
# 1. 设置 npm 镜像
npm config set registry https://registry.npmmirror.com

# 2. 设置 Electron 镜像（临时）
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 3. 验证
npm config get registry

# 4. 现在可以安装和打包了
npm install
npm run build
```

## ⚠️ 注意事项

1. **npm 镜像设置是永久的**，会一直生效直到你改回来
2. **Electron 镜像（export）是临时的**，只对当前终端窗口有效
3. 如果想 Electron 镜像永久生效，需要添加到配置文件（见方法二）

## 🎯 推荐配置

**最简单的方式**（推荐新手）：

```bash
# 一次性设置
npm config set registry https://registry.npmmirror.com
export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 然后正常使用
npm install
npm run build
```

这样设置后，npm 包和 Electron 都会从国内镜像下载，速度会快很多！

---

**提示**：设置完成后，可以关闭终端重新打开验证，npm 镜像设置会保留，Electron 镜像需要重新设置（或添加到配置文件）。



















