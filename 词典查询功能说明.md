# 🔍 词典查询功能说明

## 功能概述

词典查询功能已完全集成到 PET 魔法英语屋应用中，支持多种查询方式和完整的生词本集成。

## 第一部分：前端实现

### 1. 搜索入口设计

**位置：** 主页顶部，标题下方，进度面板上方

**功能：**
- 固定搜索框，带有搜索图标 🔍
- 支持直接输入单词查询
- 按 Enter 键或点击搜索图标即可查询

### 2. 文本高亮查询

**支持位置：**
- 📖 阅读理解模块的文本
- 🧩 完形填空模块的文本

**使用方法：**
- **桌面端：** 双击文本中的单词即可弹出查询气泡
- **移动端：** 长按单词（500ms）弹出查询气泡

**功能特点：**
- 自动高亮文本中的单词（长度>2的英文单词）
- 查询气泡显示简要释义
- 点击"详细查询"按钮打开完整词典结果

### 3. 搜索结果模态框

**组件结构：**
- 单词标题 + 音标 + 发音按钮
- 标签页切换（释义 / 例句）
- 详细释义列表（按词性分类）
- 例句展示
- 操作按钮（关闭 / 添加到生词本）

## 第二部分：API 集成

### 1. API 选型

**主API：Free Dictionary API**
- **URL：** `https://api.dictionaryapi.dev/api/v2/entries/en/{word}`
- **特点：**
  - ✅ 完全免费，无需API密钥
  - ✅ 提供音标、词性、释义、例句
  - ✅ 支持多种词性
  - ✅ 响应速度快
  - ⚠️ 仅支持英文单词

**备用API建议（如需扩展）：**
1. **Oxford Dictionaries API**
   - 商业级API，需要注册和API密钥
   - 提供更详细的释义和用法说明
   - 适合商业用途

2. **Merriam-Webster API**
   - 美国权威词典
   - 需要API密钥
   - 提供丰富的同义词和反义词

3. **Google Translate API**（用于长句翻译）
   - 支持多语言翻译
   - 需要API密钥和付费
   - 适合背景知识查询

### 2. 缓存策略

**实现方式：** localStorage 缓存

**缓存机制：**
- 缓存有效期：7天
- 最大缓存数量：100条（自动清理最旧的）
- 缓存键：`petDictCache`
- 缓存结构：
  ```javascript
  {
    "word": {
      "data": { /* 格式化后的词典数据 */ },
      "timestamp": 1234567890
    }
  }
  ```

**缓存逻辑：**
1. 查询前先检查缓存
2. 如果缓存存在且未过期，直接返回
3. 如果缓存过期或不存在，调用API
4. API返回后保存到缓存

### 3. 数据清洗与聚合

**统一数据模型：**
```javascript
{
  word: "单词",
  phonetic: "音标",
  meanings: [
    {
      partOfSpeech: "词性",
      definitions: [
        {
          definition: "释义",
          example: "例句"
        }
      ],
      synonyms: ["同义词"],
      antonyms: ["反义词"]
    }
  ],
  source: "Free Dictionary API"
}
```

**数据转换：**
- 从API原始数据提取音标、词性、释义、例句
- 统一格式便于前端展示
- 处理缺失字段（提供默认值）

## 第三部分：系统整合

### 1. 生词本集成

**数据流程：**
1. 用户点击"添加到生词本"按钮
2. 前端提取单词、释义、例句
3. 检查是否已存在（避免重复）
4. 添加到 `data.vocab` 数组
5. 保存到 localStorage
6. 更新UI统计

**数据结构：**
```javascript
{
  word: "单词",
  meaning: "第一个释义",
  example: "第一个例句或默认例句"
}
```

**集成位置：**
- 自动添加到"单词背诵"模块
- 可在单词背诵模块中正常使用
- 支持导入导出功能

### 2. 打卡与积分联动

**积分规则：**
- 首次使用词典查询：+5 积分 ⭐
- 首次添加生词本：+5 积分 ⭐
- 后续添加生词本：+2 积分 ⭐

**实现逻辑：**
```javascript
// 首次使用搜索
if (!hasUsedDictSearch) {
    hasUsedDictSearch = true;
    addReward(5, "首次使用词典查询");
}

// 添加生词本
if (!hasAddedToVocab) {
    hasAddedToVocab = true;
    addReward(5, "首次添加生词本");
} else {
    addReward(2, "添加生词本");
}
```

**积分记录：**
- 自动记录到打卡记录
- 显示在积分面板
- 计入每日/每周/每月统计

## 使用指南

### 方法一：搜索框查询
1. 在顶部搜索框输入单词
2. 按 Enter 或点击搜索图标
3. 查看详细结果
4. 可选择添加到生词本

### 方法二：双击查询
1. 在阅读理解或完形填空模块
2. 双击任意单词
3. 弹出查询气泡（显示简要信息）
4. 点击"详细查询"查看完整结果

### 方法三：长按查询（移动端）
1. 在文本中长按单词
2. 弹出查询气泡
3. 点击"详细查询"查看完整结果

## 技术细节

### 前端事件处理

**双击查询：**
```javascript
element.addEventListener('dblclick', (e) => {
    const selection = window.getSelection();
    const text = selection.toString().trim();
    if (text && /^[a-zA-Z]+$/.test(text)) {
        showWordPopup(e, text);
    }
});
```

**长按查询：**
```javascript
let longPressTimer;
element.addEventListener('touchstart', (e) => {
    longPressTimer = setTimeout(() => {
        const word = target.textContent.trim();
        if (word && /^[a-zA-Z]+$/.test(word)) {
            showWordPopup(e, word);
        }
    }, 500);
});
```

### 缓存实现

**检查缓存：**
```javascript
if (dictCache[wordLower]) {
    const cached = dictCache[wordLower];
    if (Date.now() - cached.timestamp < 7 * 24 * 60 * 60 * 1000) {
        return cached.data; // 使用缓存
    }
}
```

**保存缓存：**
```javascript
dictCache[wordLower] = {
    data: formatted,
    timestamp: Date.now()
};
saveDictCache();
```

## 注意事项

1. **网络要求：** 首次查询需要网络连接，后续查询可使用缓存
2. **API限制：** Free Dictionary API 无官方限制，但建议控制请求频率
3. **缓存清理：** 缓存超过100条会自动清理最旧的记录
4. **浏览器兼容：** 需要支持 `fetch` API 和 `localStorage`
5. **发音功能：** 使用浏览器内置的 `speechSynthesis` API

## 未来扩展建议

1. **离线词典：** 集成本地词典数据库，支持离线查询
2. **多语言支持：** 支持查询其他语言的单词
3. **学习记录：** 记录查询历史，分析学习习惯
4. **智能推荐：** 根据查询历史推荐相关单词
5. **语音输入：** 支持语音输入查询单词
















